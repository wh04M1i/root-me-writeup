## Challenge name: ELF x86 - 0 protection

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Cracking/ELF-x86-0-protection](https://www.root-me.org/en/Challenges/Cracking/ELF-x86-0-protection)

**After downloading the challange file we get a zip file and after extracting it we can see a binary file.**

**Lets see what it does executing that binary file.. So it asks for password and if incorrect then returns bad message so I assume it may use simple strcmp() cuz its common method to compare our input with flag in c as challenge says its written in c so if you have programmed in c a little you should know things so lets use this tool to hook that strcmp(<our_input>, <actual_flag>):**

```
ltrace ./ch1.bin
```

**Looks like we have solved the challenge but the texts in binaries are stored in .data, .rodata, .bss, .text sessions and so on if you have coded in asm then you should have seen one of em they store texts and binary like strings take every strings in binary out for you..**

```
strings ch1.bin
```

**We can use this "123456789" as flag/passwd to validate the challenge..**

**Thankyou.**
