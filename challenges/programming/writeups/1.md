## Challenge name: TCP - Back to school


<img width="1454" height="801" alt="image" src="https://github.com/user-attachments/assets/bc13df80-f943-4727-96af-829fe717caec" />
<br>
<br>

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Programming/TCP-Back-to-school](https://www.root-me.org/en/Challenges/Programming/TCP-Back-to-school)
<br>
<br>

**The challenge is to create a TCP connection to the server, do some computation and send the result back to the server lets see how it looks like..**

<br>
<img width="1268" height="314" alt="Screenshot From 2026-01-30 16-11-23" src="https://github.com/user-attachments/assets/4bfc548f-2253-4bd0-9e2e-6d6fc298bc17" />
<br>
<br>

**Let's code in python to solve this and our solver looks like this:**


```
import socket
 
host = "challenge01.root-me.org"
port = 52002
 
s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
s.connect((host, port))
 
data = s.recv(1024)
 
# Extract the numbers from the string
number1 = data.decode().split("Calculate the square root of ")[1].split(" and multiply by ")[0]
number2 = data.decode().split(" and multiply by ")[1].split(" =")[0]
 
# Calculate the result
result = str(round(int(number1)**0.5 * int(number2), 2))+'\n'
 
# Send the result back to the server
s.sendall(result.encode())

# Get the response from the server
data = s.recv(1024)
print(data.decode())
```

<br>

**Lets execute it and see what server replyes after our correct response inside 2 seconds..**


<img width="1268" height="314" alt="image" src="https://github.com/user-attachments/assets/807b81f5-0ba3-414a-ad4f-a0a280097348" />
<br>
<br>

**As it says we can use "RM{TCP_C0nnecT_4nD_m4Th}" as flag/passwd to solve the challenge.**<br>
**Thankyou.**
