## Challenge name: HTTP - IP restriction bypass

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/HTTP-IP-restriction-bypass](https://www.root-me.org/en/Challenges/Web-Server/HTTP-IP-restriction-bypass)

**After we start the challenge we see a login page is being shown because we are not a part of intranet. It says your ip doesn't belong to the LAN.**

**We will fire up burp to analyze HTTP requests and play around with it..**

**I assume the application first checks whether the client belongs to the intranet. There is HTTP header called X-Forwarded-For that carries the original client IP addr when req passes through proxies.**
**We will use this as payload with a random intranet ip: `X-Forwarded-For: 192.168.1.42`**

**And there you go, this was the challenge and we can use "Ip_$po0Fing" as flag/passwd to solve the challenge.**

**Thankyou.**
