## Challenge name: PHP - Command injection

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/PHP-Command-injection](https://www.root-me.org/en/Challenges/Web-Server/PHP-Command-injection)

**After starting the challenge we land on page which says submit ip to ping. So we try to ping a service like localhost(127.0.0.1).**

**So I assume under the hood php is doing something like system("ping -c 3 $ip");**

**In the Linux terminal, we can use operators like &&, ;, || to inject commands.**

**We will use this payload `127.0.0.1; ls -a` and see if we can run our arbitrary commands on the server.**

**And it's a success. We can see .passwd file so I guess its our flag lets run cat on it to see its contents.**

**We can use this "S3rv1ceP1n9Sup3rS3cure" as flag/passwd to solve the challenge.**

**Thankyou.**
