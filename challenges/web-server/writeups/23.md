## Challenge name: SQL Truncation

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/SQL-Truncation](https://www.root-me.org/en/Challenges/Web-Server/SQL-Truncation)

**The description says that we have to retrieve access to the administration's zone.**

**With SQL's default settings, input can be truncated if it's too long. Extra spacing or characters may be removed, so a value like "admin AAAA..." could end up being saved as "admin".**

**Payload: user = admin + lots of spaces + A, pass = 123456789**

**After registering and entering 123456789 as password in administrator portal we are in as admin.**

**We can use this "J41m3Qu4nD54Tr0nc" as flag/passwd to validate the challenge..**

**Thankyou.**
