## Challenge name: HTTP - Improper redirect

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/HTTP-Improper-redirect](https://www.root-me.org/en/Challenges/Web-Server/HTTP-Improper-redirect)

**After starting the challenge we can see a login page and url seems to have ?redirect.**

**The lesson is developer should use exit() or die() after redirection because the page continues to execute code below if not done that.**

**We got the flag/passwd as "ExecutionAfterRedirectIsBad" we can use this to validate.**

**Thankyou.**
