## Challenge name: PHP - Path Truncation


<img width="1344" height="420" alt="image" src="https://github.com/user-attachments/assets/83b32467-a8f5-434b-babd-3301dcfaa0c5" />
<br>
<br>

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/PHP-Path-Truncation](https://www.root-me.org/en/Challenges/Web-Server/PHP-Path-Truncation)
<br>
<br>

**As we start the challenge we can see two pages with links:**

```
http://challenge01.root-me.org/web-serveur/ch35/index.php?page=home
http://challenge01.root-me.org/web-serveur/ch35/admin.html
```

<br>
<img width="1344" height="420" alt="image" src="https://github.com/user-attachments/assets/5093be7e-073d-4336-9c25-08f9ccdd0485" />
<br><br>
<img width="1344" height="420" alt="image" src="https://github.com/user-attachments/assets/a64b0662-dfe6-4a52-a567-8ed63af36740" />
<br>
<br>

**In which admin.html is prohibited from accessing. So the article asks to find a way to read the admin.html file to get the flag. The first idea is to exploit the parameter that passes into the page to read the admin.html file**
<br><br>
**The next discovery was it seems that the website has been programmed automatically adding '.php' after the page parameter received from the request as `?page=home -> home.php` in current folder we can conform that visiting home.php removing `index.php?page=`.**

<br>
<img width="1344" height="420" alt="image" src="https://github.com/user-attachments/assets/4978c7ce-a6d7-4194-99eb-29218c19e46f" />
<br>
<br>

**So the `index?page=` is programmed nicely that %00 null byte doesn't removes the .php form `?index?page=./admin.html%00.php`.**
<br><br>
**After researching a bit on truncation in php as the challange name suggests I found out that if the request is greater than 4096 bytes then it truncates/removes it works till php version 5.x.x so I viewed page technologies using wappalyzer and the php version was same as our requirement.**

<br>
<img width="1344" height="420" alt="image" src="https://github.com/user-attachments/assets/370d1665-1952-4faa-bfa4-d36054726798" />
<br>
<br>

**When researching, based on the path truncation documentation - it was found that we cannot attack on it by giving the absolute path but can by giving a path that is not real so we will use `x/../admin.html` which resolves to `admin.html` and its not a actual path as required. And we will use `/././././` as padding at end because it resolves to same current dir .Now we will craft a bash payload:**
<br><br>
```bash
#!/usr/bin/env bash
BASE_URL="http://challenge01.root-me.org/web-serveur/ch35/index.php?page=x/../admin.html/"
PAYLOAD=$(printf './%.0s' {1..2048})  # ./ = 2 bytes and ./ × 4096 = 8192 bytes so 2048 times
curl "${BASE_URL}${PAYLOAD}"
```

<br>
<img width="1324" height="744" alt="image" src="https://github.com/user-attachments/assets/3ca7d90e-3f2e-45ee-a88c-c5d0c544c117" />
<br>
<br>

**We are able to fetch the content of admin.html So, we will use this "110V3TrUnC4T10n" as flag/passwd to validate..** <br>
**Thankyou.**
