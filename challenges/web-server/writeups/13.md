## Challenge name: HTTP - Verb tampering


<img width="1400" height="361" alt="image" src="https://github.com/user-attachments/assets/e1a0a3d7-5485-497f-8ab6-b2a84578881d" />
<br>
<br>

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/HTTP-verb-tampering](https://www.root-me.org/en/Challenges/Web-Server/HTTP-verb-tampering)
<br>
<br>

**After we start the challenge we are prompted with a login page in a web as shown bellow:**

<br>
<img width="1400" height="450" alt="image" src="https://github.com/user-attachments/assets/d5ab63e8-df0a-49fe-a5da-667ff20275a5" />
<br>
<br>

**Now let's understand whats verb tampering lets take a example with following self explaned example php code:**

```
<?php
session_start();

$valid_user = "admin";
$valid_pass = "admin123";

if ($_SERVER["REQUEST_METHOD"] === "GET") {
    $user = $_POST["username"] ?? "";
    $pass = $_POST["password"] ?? "";

    if ($user === $valid_user && $pass === $valid_pass) {
       echo "<script>alert('login successo!');</script>";
    }
} else {
    echo "login failed";
    die();
}

<!DOCTYPE html>
<html>
<head>
    <title>MyBank</title>
</head>
<body>

<h2>Welcome to Bank</h2>

<p>Account Holder: Admin</p>
<p>Balance: $10,000</p>

<hr>

<h3>Transfer Money</h3>

<form>
    <input placeholder="Recipient Account">
    <input placeholder="Amount">
    <button>Transfer</button>
</form>

</body>
</html>

```

**In that code there is verb tempering vuln cuz its only checking if request method is get and if yes its checking user pass and if incorrect it exits in else statement but if request method is somehtin else then we will get access to site. eg:**


**Original:**

``` GET /vuln.php?username=user&password=pass ```

**Bypass:**

``` ASDF /vuln.php?username=user&password=pass ```
<br><br>
**This will log us in so lets try on our live target. And in the request its using base64 hash to store our creds just for kind info..**


<img width="1525" height="580" alt="image" src="https://github.com/user-attachments/assets/86ec57db-908e-4b28-a3c8-95e9fcdced1f" />
<br>
<br>

**That's it we got the flag/passwd as "a23e$dme96d3saez$$prap" which we can use to validate.**
<br>
**Thankyou.**
