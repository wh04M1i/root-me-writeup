## Challenge name: HTTP - Verb tampering

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/HTTP-verb-tampering](https://www.root-me.org/en/Challenges/Web-Server/HTTP-verb-tampering)

**After we start the challenge we are prompted with a login page.**

**Verb tampering vulnerability occurs when the code only checks for specific request methods. If we change the HTTP method (e.g., GET to POST, or a custom method), we might bypass authentication checks.**

**We got the flag/passwd as "a23e$dme96d3saez$$prap" which we can use to validate.**

**Thankyou.**
