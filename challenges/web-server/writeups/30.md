## Challenge name: JWT - Unsecure Key Handling

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/JWT-Unsecure-Key-Handling](https://www.root-me.org/en/Challenges/Web-Server/JWT-Unsecure-Key-Handling)

**After starting the challenge we can see a web page titled shop-me with marketplace and admin area.**

**Decode the JWT token - note the `jku` parameter. Create a new RSA key, host the JWK, and modify the JWT to change user from guest to admin.**

**The JKU must start with `http://localhost`. Use `http://localhost@pastebin.com` to bypass.**

**We can use this "RM{4lw4y5_ch3ck_th3_JKU_C0rr3ctly}" as flag/passwd to validate the challenge..**

**Thankyou.**
