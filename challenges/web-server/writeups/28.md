## Challenge name: NoSQL injection - Authentication


<img width="1324" height="442" alt="image" src="https://github.com/user-attachments/assets/75d21837-1fa1-4023-9a68-961cb785db15" />
<br>
<br>

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/NoSQL-injection-Authentication](https://www.root-me.org/en/Challenges/Web-Server/NoSQL-injection-Authentication)
<br>
<br>

**After we start the challenge we can see a login page so lets try to login with admin%admin or admin%password let's observe what happens.**

<br>
<img width="1324" height="348" alt="image" src="https://github.com/user-attachments/assets/ea9f9ef1-fb98-4155-bf8b-56a461e039f7" />
<br><br>
<img width="1324" height="348" alt="image" src="https://github.com/user-attachments/assets/234ea380-6e44-4fc5-ba12-fef04b6ffcd8" />
<br>
<br>

**Currently we are in `http://challenge01.root-me.org/web-serveur/ch38/?login=admin&pass=password` and it says `Bad username or bad password !`. So lets try no sql in url we can send `login[$eq]=admin&pass[$ne]=whatever` in request where $eq means equals to our input as admin and that users exists -> true and $ne mens not equal to our input -> true cuz the passwd we entered is worng so both conditions returns true and we will be in. Let's see..**
<br><br>
```
http://challenge01.root-me.org/web-serveur/ch38/?login[$eq]=admin&pass[$ne]=whatever // no need to open brup it's a get request so just modify url..
```

<br>
<img width="1324" height="348" alt="image" src="https://github.com/user-attachments/assets/7dc396f6-c778-4705-b055-d34126fa0638" />
<br>
<br>

**But the question is where is the validation flag/passwd? and as always my bad habit I forgot the read challenge statement which says find the username of the hidden user. MongoDB is query model so we can't dump everything form database and retrive username that we don't know but we can try many different thigns like let's try regix match in username using following payload:**
<br><br>
```
Payload: login[$regex]=^a&pass[$ne]=whatever
Explan: $regex lets us match usernames by pattern (^ start, $ end) as we dod ^a so if ther is user whose username starts with a then we are in as that usr..
```
<br>

**We will find hidden user by searching usernames starting from `a` to `z` and when we reach `f` we can see that user starting with `f` exists as `flag{<flag>}` and it gives us the flag.**

<br>
<img width="1324" height="348" alt="image" src="https://github.com/user-attachments/assets/8acb7028-5661-4f50-8039-0411d445d32e" />
<br>
<br>

**We can use this "nosqli_no_secret_4_you" as flag/passwd to validate the challenge..** <br>
**Thankyou.**
