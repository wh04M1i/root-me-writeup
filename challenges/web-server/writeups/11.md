## Challenge name: Nginx - Alias Misconfiguration

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/Nginx-Alias-Misconfiguration](https://www.root-me.org/en/Challenges/Web-Server/Nginx-Alias-Misconfiguration)

**After starting the challenge we can see a login page.**

**There is a directory listing at /assets/.**

**Lets understand Alias Misconfiguration (Off-By-Slash) in nginx:**
```
root /var/www/

location /assets {
    alias /var/www/assets;
}
```

**Lets see if we get any juicy info with directory traversal from off-by-slash/alias-misconfig vulnerability by visiting `/assets../`**

**We can see flag.txt there. That's the challenge.**

**You can use this "RM{4lias_M1sC0nf_HuRtS!}" as flag/passwd to validate.**

**Thankyou.**
