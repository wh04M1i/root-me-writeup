## Challenge name: Elixir - EEx


<img width="1340" height="357" alt="image" src="https://github.com/user-attachments/assets/0f93726a-5c5d-43aa-b065-9197dfdbd470" />
<br>
<br>

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/Elixir-EEx](https://www.root-me.org/en/Challenges/Web-Server/Elixir-EEx)
<br>
<br>

**After we start the challenge we can see that source code of the challenge was given. Lets have a look on it.**

<br>
<img width="1340" height="689" alt="image" src="https://github.com/user-attachments/assets/1def7b53-4d66-4558-9a0c-4607f8395794" />
<br><br>
<img width="1340" height="150" alt="image" src="https://github.com/user-attachments/assets/b2413c14-43f7-4484-9c32-c6370d0b12a6" />
<br>
<br>

**The challenge name says Elixir so lets here some words on it. After researching a bit found out that - It is a functional language built on Erlang's VM for scalable, fault-tolerant, concurrent apps. In this case our web app is using this language in the backend and giving us upload fourm. And `?page=` parameter is renderring .eex files form `cloud_service/lib/views/upload.eex`. and after testing also found that no protection was implemented on any functionality in the web.**

<br>
<img width="1340" height="150" alt="image" src="https://github.com/user-attachments/assets/da6a559b-707a-4a77-916d-7a866d72b8c6" />
<br>
<br>

**So we know that there is no security on uploads and so on if we upload our own .eex file it will be uploaded in `uploads` its 2 files back from `?page=` parameter as we have the source code. Now lets try uploading a .eex file to test it out and check on `../../uploads/uploaded_name` without .eex format because the `?page` parm renders things inside view folder without .eex format.**

```
file: test.eex
payload: <%= 2 * 2 %>
```

<br>
<img width="1340" height="377" alt="image" src="https://github.com/user-attachments/assets/9abe65ac-12e5-40d4-ac67-0526633074ec" />
<br><br>
<img width="1413" height="377" alt="image" src="https://github.com/user-attachments/assets/dddd7858-2625-401c-b294-219ccf3b0803" />
<br>
<br>

**It was renamed btw after upload but no problem now lets visit `http://challenge01.root-me.org:59089/?page=../../uploads/93c24238-dde8-4f03-9796-062f425a49b5` and see what happens.**

<br>
<img width="1413" height="377" alt="image" src="https://github.com/user-attachments/assets/2dd4ddc8-3d0c-4c0e-98e0-ea380e9f0dcd" />
<br>
<br>

****Templating in Elixir is handled by EEx, which lets you embed Elixir code inside HTML or text. By whiich we can give a template code in .eex and the templating mechanism runs it and shows in web. By multible vulnerabilities such as `path traversal` + `unsafe template rendering` it leads to `SSTI` and then `RCE`. This time insted of `<%= 2 * 2 %>` lets upload `<%= :os.cmd('ls -ali') %>` in .eex file.****

<br>
<img width="1413" height="377" alt="image" src="https://github.com/user-attachments/assets/af7f3918-e32f-472e-bb69-23edcd5a2060" />
<br>
<br>

**Now lets visit `http://challenge01.root-me.org:59089/?page=../../uploads/<GENERATED_FILENAME_AFTER_UPLOAD>` and check out.**

<br>
<img width="1413" height="377" alt="image" src="https://github.com/user-attachments/assets/3bb46107-8ac9-42a9-8167-3f582c17104f" />
<br>
<br>

**As we can see flag.txt file is there so lets upload `<%= :os.cmd('cat ./flag.txt') %>` and retrive our flag but according to the source code the flag was on `../` but whatever lets retrive its contents.**

<br>
<img width="1413" height="377" alt="image" src="https://github.com/user-attachments/assets/5033dea2-5132-4db2-9e9d-72e0156c1cd6" />
<br>
<br>

**Got the flag.txt contents and can use this "RM{EEx_C@n_b3_DanGeR0u$:)!} " as flag/passwd to validate the challenge..** <br>
**Thankyou.**
