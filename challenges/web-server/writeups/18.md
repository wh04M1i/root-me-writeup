## Challenge name: API - Broken Access

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Web-Server/API-Broken-Access](https://www.root-me.org/en/Challenges/Web-Server/API-Broken-Access)

**After starting the challenge we can see a page where we can create users, login, view user with user id and so on.**

**After registering and logging in, we can try to access other users data via IDOR. By manipulating the API request we can fetch user with id 1.**

**We got the flag/passwd as "RM{E4sy_1d0r_0n_API}".**

**Thankyou.**
