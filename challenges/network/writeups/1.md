## Challenge name: FTP - authentication


<img width="1376" height="438" alt="image" src="https://github.com/user-attachments/assets/76230535-2f41-487c-a6da-1d4a2fcbb4f1" />
<br>
<br>

``` Challenge Link:``` [https://www.root-me.org/en/Challenges/Network/FTP-authentication](https://www.root-me.org/en/Challenges/Network/FTP-authentication)
<br>
<br>

**We have to download the .pcap file given to start the challenge that is a file type which stores network traffic packates and the objective is straightforward: inspect the captured traffics and extract the password used by the user during FTP authentication.**

<br>
<img width="1361" height="284" alt="image" src="https://github.com/user-attachments/assets/336ffd80-8e27-43a1-b2cf-1fc95e9c98f1" />
<br>
<br>

**So, we have downloaded the .pcap file and to analyze it we have to use tools like wireshark or tshark which is tui version of wireshark and we will use that for this challaenge. Use command bellow to read things inside that file:**


```
tshark -r ch1.pcap 
```

**In the command -r stands for reading a already captured fine normally we are not starting tshark for capturing in real time or something..**

<br>
<img width="1361" height="284" alt="image" src="https://github.com/user-attachments/assets/4443fef2-76eb-4eb6-87ee-7791ef6d8e9e" />
<br>
<br>

**After running it we can see that it throws a bunch of texts which are mixed with different kind of protocal requestes captured since we know that we are finding ftp authentication details and it doesn't encoads passwords so we can pipe output to grep to make our life easier and get the hardcoaded creds using:**


```
tshark -r ch1.pcap | grep FTP
```

<br>
<img width="1481" height="673" alt="image" src="https://github.com/user-attachments/assets/240ecd90-cddb-4fc7-83a3-695889f12f14" />
<br>
<br>

**We can observe that one had used cdts3500 as user & pass to login uisng ftp.**

**So, we can use this "cdts3500" as flag/passwd to validate the challenge..** <br>
**Thankyou.**
